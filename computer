if [ "$1" = "clean" ]
then
	mvn clean && rm -rf target
elif [ ! -f "target/classes/leng1/Computadora.class" ]
then
	mvn clean package &&
	java -cp target/classes leng1.Computadora
else
	java -cp target/classes leng1.Computadora
fi
